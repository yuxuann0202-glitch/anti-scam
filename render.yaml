services:
  - type: web
    name: anti-scam-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
